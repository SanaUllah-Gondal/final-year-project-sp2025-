// Flutter UI File 26
import 'package:flutter/material.dart';

void main() => runApp(App26());

class App26 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: Scaffold(body: Center(child: Text('UI 26'))));
  }
}