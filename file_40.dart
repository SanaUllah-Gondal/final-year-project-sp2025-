// SkillLink Code File 40
import 'package:flutter/material.dart';

void main() {
  runApp(SkillLinkApp40());
}

class SkillLinkApp40 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text('SkillLink File 40')),
        body: Center(child: Text('Hello from SkillLink!')),
      ),
    );
  }
}
