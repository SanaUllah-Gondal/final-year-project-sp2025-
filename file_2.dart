// SkillLink Code File 2
import 'package:flutter/material.dart';

void main() {
  runApp(SkillLinkApp2());
}

class SkillLinkApp2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text('SkillLink File 2')),
        body: Center(child: Text('Hello from SkillLink!')),
      ),
    );
  }
}
