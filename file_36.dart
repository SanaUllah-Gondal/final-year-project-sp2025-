// SkillLink Code File 36
import 'package:flutter/material.dart';

void main() {
  runApp(SkillLinkApp36());
}

class SkillLinkApp36 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text('SkillLink File 36')),
        body: Center(child: Text('Hello from SkillLink!')),
      ),
    );
  }
}
