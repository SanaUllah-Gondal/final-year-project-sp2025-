// Flutter UI File 46
import 'package:flutter/material.dart';

void main() => runApp(App46());

class App46 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: Scaffold(body: Center(child: Text('UI 46'))));
  }
}