// Flutter UI File 98
import 'package:flutter/material.dart';

void main() => runApp(App98());

class App98 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: Scaffold(body: Center(child: Text('UI 98'))));
  }
}