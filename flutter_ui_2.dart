// Flutter UI File 2
import 'package:flutter/material.dart';

void main() => runApp(App2());

class App2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: Scaffold(body: Center(child: Text('UI 2'))));
  }
}