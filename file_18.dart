// SkillLink Code File 18
import 'package:flutter/material.dart';

void main() {
  runApp(SkillLinkApp18());
}

class SkillLinkApp18 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text('SkillLink File 18')),
        body: Center(child: Text('Hello from SkillLink!')),
      ),
    );
  }
}
