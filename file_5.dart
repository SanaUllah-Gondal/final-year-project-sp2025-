// SkillLink Code File 5
import 'package:flutter/material.dart';

void main() {
  runApp(SkillLinkApp5());
}

class SkillLinkApp5 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text('SkillLink File 5')),
        body: Center(child: Text('Hello from SkillLink!')),
      ),
    );
  }
}
