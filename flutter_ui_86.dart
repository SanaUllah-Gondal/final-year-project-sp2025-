// Flutter UI File 86
import 'package:flutter/material.dart';

void main() => runApp(App86());

class App86 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: Scaffold(body: Center(child: Text('UI 86'))));
  }
}