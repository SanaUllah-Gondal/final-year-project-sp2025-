// SkillLink Code File 42
import 'package:flutter/material.dart';

void main() {
  runApp(SkillLinkApp42());
}

class SkillLinkApp42 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text('SkillLink File 42')),
        body: Center(child: Text('Hello from SkillLink!')),
      ),
    );
  }
}
