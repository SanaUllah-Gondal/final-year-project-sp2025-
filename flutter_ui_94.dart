// Flutter UI File 94
import 'package:flutter/material.dart';

void main() => runApp(App94());

class App94 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: Scaffold(body: Center(child: Text('UI 94'))));
  }
}