// SkillLink Code File 21
import 'package:flutter/material.dart';

void main() {
  runApp(SkillLinkApp21());
}

class SkillLinkApp21 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text('SkillLink File 21')),
        body: Center(child: Text('Hello from SkillLink!')),
      ),
    );
  }
}
