// SkillLink Code File 10
import 'package:flutter/material.dart';

void main() {
  runApp(SkillLinkApp10());
}

class SkillLinkApp10 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text('SkillLink File 10')),
        body: Center(child: Text('Hello from SkillLink!')),
      ),
    );
  }
}
