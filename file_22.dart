// SkillLink Code File 22
import 'package:flutter/material.dart';

void main() {
  runApp(SkillLinkApp22());
}

class SkillLinkApp22 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text('SkillLink File 22')),
        body: Center(child: Text('Hello from SkillLink!')),
      ),
    );
  }
}
