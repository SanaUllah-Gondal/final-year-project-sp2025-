// SkillLink Code File 26
import 'package:flutter/material.dart';

void main() {
  runApp(SkillLinkApp26());
}

class SkillLinkApp26 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text('SkillLink File 26')),
        body: Center(child: Text('Hello from SkillLink!')),
      ),
    );
  }
}
